<MudAlert Severity="Severity.Info">
    Scegli un numero tra <strong>@MatchLimits.Min</strong> e <strong>@MatchLimits.Max</strong> (esclusi).
    @* Il numero segreto è compreso tra <strong>@MatchLimits.Min</strong> e <strong>@MatchLimits.Max</strong> (eclusi) *@
</MudAlert>


@code {
    [Parameter, EditorRequired]
    public required Limits InitialLimits { get; set; }

    [Parameter, EditorRequired]
    public required Limits MatchLimits { get; set; }
}
