<MudText>
    Il numero segreto è compreso tra @MatchLimits.Min e @MatchLimits.Max
</MudText>


@code {
    [Parameter, EditorRequired]
    public required Limits InitialLimits { get; set; }

    [Parameter, EditorRequired]
    public required Limits MatchLimits { get; set; }
}
